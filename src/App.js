import React from 'react';
import { Counter, Buttons, Options } from './components';

const App = () => (
  <div style={{textAlign: 'center'}}>
    <Counter/>
    <Options/>
    <Buttons/>
  </div>
);

export default App;